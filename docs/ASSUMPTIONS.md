# Assumptions & Trade-offs

A running log. Every ambiguous requirement we resolved, and every shortcut we took, lives here
with the reasoning. Code shortcuts are also tagged `# SCALE-DEBT:` at the call site.

## Product & scope

| # | Assumption | Why |
|---|---|---|
| A1 | The fictional company is Commercial Bank; all sample documents are synthetic and generated by `scripts/generate_mock_data.py`. | No real data may be used. Generation makes the corpus reproducible. |
| A2 | "Last year" in the canonical RLM query means the trailing 12 months from today, not the previous calendar year. | This matches how an operations lead would phrase it. The resolved window is visible in the Python plan. |
| A3 | Offline mode (`LLM_PROVIDER=none`) is a supported degraded mode, not the primary experience. | See ADR-0001. The UI shows `degraded` components on every answer. |
| A4 | RLM extraction is shaped for incident reports (root cause, impact). | This is the canonical demo. Other research questions pass the best passages to synthesis without structured extraction (ADR-0003). |
| A5 | `RLM_MAX_DOCS_PER_SLICE` defaults to 2 so recursion is visible on a 31-document corpus. | Production corpora should raise it (e.g. 10–20). |
| A6 | Chat answers are grounded only in retrieved evidence; the assistant declines instead of answering from general knowledge. | For a regulated bank, an uncited answer is worse than no answer. |

## Security & RBAC

| # | Assumption | Why |
|---|---|---|
| S1 | Document access levels are ordered `public < internal < confidential < restricted`. Clearance: Viewer → public, internal; Analyst → + confidential; Administrator → + restricted. | CLAUDE.md defines tool permissions but not document clearance. A strict ordering is the common bank classification model. |
| S2 | Auth uses Option A: hardcoded demo users (PBKDF2 hashes) issuing HS256 JWTs. The role is re-resolved from the directory on every request. | Proves server-side RBAC without operating Keycloak. Swapping to OIDC only changes `auth/`. |
| S3 | HITL approval is given by the requesting administrator. | POC confirmation step. Production should require a second approver (four-eyes). See SECURITY.md §7. |
| S4 | The MCP server is reachable only on the internal Docker network and trusts the API. RBAC for MCP tools is enforced in the API before calling. | Keeps the MCP server simple. mTLS or service tokens are the production path. |
| S5 | Retrieved passages with injection signals are flagged, not removed. | Removing them would let an attacker suppress facts by adding trigger phrases. |
| S6 | Login attempts are throttled per username using the same token bucket as chat. | Brute-force protection without extra infrastructure. |

## Scalability debt

| # | Shortcut | Path to scale |
|---|---|---|
| D1 | Without `REDIS_URL`, the rate limiter, checkpointer, long-term memory and audit log are per-process. | Set `REDIS_URL` (Docker Compose does). `APP_ENV=production` refuses to start without it. |
| D2 | `VECTOR_STORE=memory` indexes the mock corpus inside each worker at startup. | Production uses Pinecone populated by `data/ingest.py`. |
| D3 | The document catalog (RLM explore) and BM25 statistics are JSON artifacts loaded at startup. | Move the catalog to a metadata table. Refit BM25 in the batch ingestion job. |
| D4 | The Python sandbox relies on process isolation and rlimits, without network isolation. | Run in gVisor/Firecracker or a network-less sidecar. |
| D5 | MCP client opens a connection per call. | Pool sessions per worker if MCP latency becomes significant. |
| D6 | Long-term memory recall is lexical over ≤ 50 facts per user. | Enable the store's vector index when memories grow. |

## Environment notes (this build)

| # | Note |
|---|---|
| E1 | Verified locally: 106 automated tests; ingestion dry run; a real-network smoke test (MCP server and API as separate processes, SSE stream). |
| E2 | Not verified in this environment (no API keys, full disk): live Anthropic, Pinecone and LangSmith calls, Docker image builds, and the Redis-backed checkpointer/store. Run `docker compose up --build` and the opt-in Redis test before the demo. |
